#ifndef _UTILS_HPP
#define _UTILS_HPP

#include <iostream>

#include "defines.hpp"
using namespace std;

void prompt_credentials(string& username, string& password);
int parse_input(string& input);

#endif //_UTILS_HPP
